# Infrastructure

Docker, Docker Compose, Nginx, and deployment configuration will live here.

Phase 0 only prepares the directory boundary. Concrete Dockerfiles, Compose services, and Nginx routing should be added when runnable services exist.
